# Catalog tile art (drop-in)

No art files ship yet — `CatalogCard` renders its striped placeholder until
a real image lands here. `src/utils/catalogArt.js` already points at the
expected filename below; add a file with that exact name to switch it on
(no code changes needed).

- `love-letter.png` — the Catalog page's tile art for Love Letter (used at
  240px+ wide, ~150px tall crop — a roughly 8:5 landscape image looks best)
